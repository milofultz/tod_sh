#include <ncurses.h>

int main (int argc, char *argv[]) {
    // load the tasks file
    // if argv is 
        // ls
            // print all tasksi (lines) in file
        // add
            // append (arg) to end of file
        // rm
            // remove entry on line (arg)
        // do
            // start pomodoro for task (arg)
            // when timer is done, add 1 to pomodoro count of task (arg)
        // x
            // if task (arg) is done, mark not done
            // else, mark task (arg) as done
}

